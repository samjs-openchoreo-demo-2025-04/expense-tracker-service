apiVersion: score.dev/v1b1
metadata:
  name: expense-tracker-service
  project: expense-tracker
  organization: expense-tracker-service
service:
  ports:
    default:
      port: 8080
      protocol: TCP
containers:
  container-one1:
    image: .